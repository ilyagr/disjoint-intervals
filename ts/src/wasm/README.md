This directory contains the WASM build and TypeScript bindings for the Rust implementation of disjoint-intervals-lib.

- The files are generated by `wasm-pack build` from the Rust crate.
- To update, re-run `wasm-pack build --target bundler --out-dir pkg` in the root directory and copy the output here.
